---
const navLinks = [
  { href: '/clanky', label: 'Články' },
  { href: '/trhy', label: '📊 Trhy' },
  { href: '/kalkulacka', label: 'Kalkulačky' },
  { href: '/slovnik', label: 'Slovník' },
  { href: '/index-trhu', label: 'Index trhu' },
  { href: '/o-nas', label: 'O nás' },
  { href: '/zacatecnik', label: '🎓 Začátečník' },
];

const currentPath = Astro.url.pathname;

function isActive(href: string) {
  if (href === '/') return currentPath === '/';
  return currentPath.startsWith(href);
}
---

<header id="main-nav" class="sticky top-0 z-50 transition-all duration-300 border-b border-transparent" data-scrolled="false">
  <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">

      <!-- Logo -->
      <a href="/" class="flex items-center gap-1 group">
        <span class="font-display text-2xl font-bold text-gold-primary group-hover:text-gold-light transition-colors duration-200">Byznys</span>
        <span class="font-display text-2xl font-bold text-text-primary">Denne</span>
      </a>

      <!-- Desktop nav -->
      <div class="hidden md:flex items-center gap-3 lg:gap-4">
        {navLinks.map(link => (
          <a
            href={link.href}
            class={`nav-link text-xs font-medium ${isActive(link.href) ? 'active text-gold-primary' : ''}`}
          >
            {link.label}
          </a>
        ))}
        <button id="theme-toggle" class="theme-toggle" aria-label="Přepnout světlý/tmavý režim" title="Přepnout režim">
          <svg id="theme-icon-sun" class="w-5 h-5 hidden" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="4" />
            <path stroke-linecap="round" d="M12 2v2m0 16v2M4.93 4.93l1.41 1.41m11.32 11.32l1.41 1.41M2 12h2m16 0h2M4.93 19.07l1.41-1.41m11.32-11.32l1.41-1.41" />
          </svg>
          <svg id="theme-icon-moon" class="w-5 h-5 hidden" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z" />
          </svg>
        </button>
        <a
          href="https://instagram.com/byznysdenne"
          target="_blank"
          rel="noopener noreferrer"
          class="btn-gold text-sm px-5 py-2"
        >
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
          </svg>
          Instagram
        </a>
      </div>

      <!-- Mobile controls -->
      <div class="md:hidden flex items-center gap-1">
        <button id="theme-toggle-mobile" class="theme-toggle" aria-label="Přepnout světlý/tmavý režim">
          <svg id="theme-icon-sun-m" class="w-5 h-5 hidden" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="4" />
            <path stroke-linecap="round" d="M12 2v2m0 16v2M4.93 4.93l1.41 1.41m11.32 11.32l1.41 1.41M2 12h2m16 0h2M4.93 19.07l1.41-1.41m11.32-11.32l1.41-1.41" />
          </svg>
          <svg id="theme-icon-moon-m" class="w-5 h-5 hidden" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z" />
          </svg>
        </button>
        <button id="mobile-menu-btn" class="p-2 text-text-secondary hover:text-text-primary transition-colors" aria-label="Menu">
        <svg id="icon-open" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
        </svg>
        <svg id="icon-close" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
        </svg>
        </button>
      </div>
    </div>
  </nav>

  <!-- Mobile menu -->
  <div id="mobile-menu" class="hidden md:hidden border-t border-border bg-surface/95 backdrop-blur-md">
    <div class="max-w-7xl mx-auto px-4 py-4 space-y-1">
      {navLinks.map(link => (
        <a
          href={link.href}
          class={`block px-3 py-3 rounded-lg text-sm font-medium transition-colors duration-200 ${
            isActive(link.href)
              ? 'bg-gold-primary/10 text-gold-primary'
              : 'text-text-secondary hover:text-text-primary hover:bg-card'
          }`}
        >
          {link.label}
        </a>
      ))}
      <div class="pt-2 border-t border-border">
        <a
          href="https://instagram.com/byznysdenne"
          target="_blank"
          rel="noopener noreferrer"
          class="btn-gold w-full text-sm px-5 py-3 mt-2"
        >
          Instagram
        </a>
      </div>
    </div>
  </div>
</header>

<script>
  const nav = document.getElementById('main-nav');
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  const iconOpen = document.getElementById('icon-open');
  const iconClose = document.getElementById('icon-close');

  // Scroll behaviour: add bg blur when scrolled
  window.addEventListener('scroll', () => {
    if (window.scrollY > 10) {
      nav?.classList.add('bg-background/90', 'backdrop-blur-md', 'border-border');
      nav?.classList.remove('border-transparent');
    } else {
      nav?.classList.remove('bg-background/90', 'backdrop-blur-md', 'border-border');
      nav?.classList.add('border-transparent');
    }
  }, { passive: true });

  // Mobile toggle
  btn?.addEventListener('click', () => {
    const isOpen = !menu?.classList.contains('hidden');
    menu?.classList.toggle('hidden');
    iconOpen?.classList.toggle('hidden', !isOpen);
    iconClose?.classList.toggle('hidden', isOpen);
  });

  // ---- Theme toggle (dark/light) ----
  const sun = document.getElementById('theme-icon-sun');
  const moon = document.getElementById('theme-icon-moon');
  const sunM = document.getElementById('theme-icon-sun-m');
  const moonM = document.getElementById('theme-icon-moon-m');

  function syncThemeIcons(theme: string) {
    const isDark = theme === 'dark';
    // In dark mode show the sun (click → go light); in light show the moon.
    sun?.classList.toggle('hidden', !isDark);
    moon?.classList.toggle('hidden', isDark);
    sunM?.classList.toggle('hidden', !isDark);
    moonM?.classList.toggle('hidden', isDark);
  }

  function currentTheme(): string {
    return document.documentElement.getAttribute('data-theme') || 'dark';
  }

  function setTheme(theme: string) {
    document.documentElement.setAttribute('data-theme', theme);
    try { localStorage.setItem('theme', theme); } catch (e) {}
    const meta = document.querySelector('meta[name="theme-color"]');
    meta?.setAttribute('content', theme === 'light' ? '#faf9f7' : '#0a0a0a');
    syncThemeIcons(theme);
  }

  function toggleTheme() {
    setTheme(currentTheme() === 'dark' ? 'light' : 'dark');
  }

  syncThemeIcons(currentTheme());
  document.getElementById('theme-toggle')?.addEventListener('click', toggleTheme);
  document.getElementById('theme-toggle-mobile')?.addEventListener('click', toggleTheme);

  // React to OS preference changes when user hasn't explicitly chosen
  window.matchMedia('(prefers-color-scheme: light)').addEventListener('change', (e) => {
    if (!localStorage.getItem('theme')) {
      setTheme(e.matches ? 'light' : 'dark');
    }
  });
</script>
